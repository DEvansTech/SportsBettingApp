export type Props = {
  selectedDate: string;
  sportName: string;
};

export type ISelections = {
  mlb: number[];
  nfl: number[];
  ncaaf: number[];
  ncaab: number[];
  nba: number[];
};

export type FavortriteType = {
  teamID: number;
  teamSort: string;
};
